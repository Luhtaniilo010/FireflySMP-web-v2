{
  "name": "fireflysmp-web",
  "version": "2.0.0",
  "private": true,
  "description": "FireflySMP website for Cloudflare Pages + Pages Functions + PostgreSQL/Hyperdrive",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
